import React, { FC } from 'react';
import RegisterForm from '@/features/auth/ui/RegisterForm/RegisterForm';

const Page: FC = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default Page;
