import React, { FC } from 'react';
import LoginForm from '@/features/auth/ui/LoginForm/LoginForm';

const Page: FC = () => {
  return (
    <div>
      <LoginForm />
    </div>
  );
};

export default Page;
